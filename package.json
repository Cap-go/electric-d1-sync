{
  "name": "electric-d1-sync",
  "module": "src/worker.ts",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy --env-from=.env",
    "deploy:dev": "wrangler deploy --env-from=.env --env=development",
    "deploy:prod": "wrangler deploy --env-from=.env --env=production"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@electric-sql/client": "^1.0.0-beta.4",
    "@types/better-sqlite3": "^7.6.12",
    "better-sqlite3": "^11.8.1"
  }
}
